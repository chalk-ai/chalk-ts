// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: chalk/auth/v1/audit.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "chalk.auth.v1";

export enum AuditLevel {
  AUDIT_LEVEL_UNSPECIFIED = 0,
  AUDIT_LEVEL_ERRORS = 1,
  AUDIT_LEVEL_ALL = 2,
  UNRECOGNIZED = -1,
}

export function auditLevelFromJSON(object: any): AuditLevel {
  switch (object) {
    case 0:
    case "AUDIT_LEVEL_UNSPECIFIED":
      return AuditLevel.AUDIT_LEVEL_UNSPECIFIED;
    case 1:
    case "AUDIT_LEVEL_ERRORS":
      return AuditLevel.AUDIT_LEVEL_ERRORS;
    case 2:
    case "AUDIT_LEVEL_ALL":
      return AuditLevel.AUDIT_LEVEL_ALL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuditLevel.UNRECOGNIZED;
  }
}

export function auditLevelToJSON(object: AuditLevel): string {
  switch (object) {
    case AuditLevel.AUDIT_LEVEL_UNSPECIFIED:
      return "AUDIT_LEVEL_UNSPECIFIED";
    case AuditLevel.AUDIT_LEVEL_ERRORS:
      return "AUDIT_LEVEL_ERRORS";
    case AuditLevel.AUDIT_LEVEL_ALL:
      return "AUDIT_LEVEL_ALL";
    case AuditLevel.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface AuditOptions {
  level: AuditLevel;
  description: string;
}

function createBaseAuditOptions(): AuditOptions {
  return { level: 0, description: "" };
}

export const AuditOptions: MessageFns<AuditOptions> = {
  encode(message: AuditOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.level !== 0) {
      writer.uint32(8).int32(message.level);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.level = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditOptions {
    return {
      level: isSet(object.level) ? auditLevelFromJSON(object.level) : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: AuditOptions): unknown {
    const obj: any = {};
    if (message.level !== 0) {
      obj.level = auditLevelToJSON(message.level);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuditOptions>, I>>(base?: I): AuditOptions {
    return AuditOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuditOptions>, I>>(object: I): AuditOptions {
    const message = createBaseAuditOptions();
    message.level = object.level ?? 0;
    message.description = object.description ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
